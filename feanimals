-- 🐍 Slithering Snake Emote Player (FE-Style)
-- X tuşu ile menüyü aç/kapat

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hum = char:WaitForChild("Humanoid")
local animator = hum:FindFirstChildOfClass("Animator")

-- ✅ Slithering Snake raw animation asset ID
local SlitheringSnakeAnimId = "rbxassetid://74812817557392"

-- GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "SnakeEmoteGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 120)
frame.Position = UDim2.new(0.7, 0, 0.4, 0)
frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui
frame.Visible = true -- açık başlasın

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,30)
title.BackgroundColor3 = Color3.fromRGB(0,200,100)
title.Text = "🐍 Slithering Snake"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Font = Enum.Font.FredokaOne
title.TextScaled = true

-- Şu anki anim
local currentTrack = nil

-- Start butonu
local startBtn = Instance.new("TextButton", frame)
startBtn.Size = UDim2.new(1,-10,0,30)
startBtn.Position = UDim2.new(0,5,0,40)
startBtn.BackgroundColor3 = Color3.fromRGB(50,50,50)
startBtn.Text = "▶ Start Snake"
startBtn.TextColor3 = Color3.fromRGB(255,255,255)
startBtn.Font = Enum.Font.SourceSansBold
startBtn.TextScaled = true

startBtn.MouseButton1Click:Connect(function()
    if currentTrack then currentTrack:Stop() end
    local anim = Instance.new("Animation")
    anim.AnimationId = SlitheringSnakeAnimId
    local track = animator:LoadAnimation(anim)
    track.Priority = Enum.AnimationPriority.Action
    track:Play()
    currentTrack = track
end)

-- Stop butonu
local stopBtn = Instance.new("TextButton", frame)
stopBtn.Size = UDim2.new(1,-10,0,30)
stopBtn.Position = UDim2.new(0,5,0,80)
stopBtn.BackgroundColor3 = Color3.fromRGB(200,50,50)
stopBtn.Text = "⛔ Stop"
stopBtn.TextColor3 = Color3.fromRGB(255,255,255)
stopBtn.Font = Enum.Font.SourceSansBold
stopBtn.TextScaled = true

stopBtn.MouseButton1Click:Connect(function()
    if currentTrack then
        currentTrack:Stop()
        currentTrack = nil
        hum:ChangeState(Enum.HumanoidStateType.Running)
    end
end)

-- X tuşuyla aç/kapat
UIS.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.X then
        frame.Visible = not frame.Visible
    end
end)
